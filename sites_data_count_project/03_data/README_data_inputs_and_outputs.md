# 03_data/ — データ入出力

プログラムが読み書きするデータファイルを置く場所。
入力と出力を別フォルダで物理的に分離している。

## サブフォルダ
- `dictionaries/` — Janome カスタム辞書(プログラムへの入力)
- `output/` — プログラム実行で生成されるファイル(.xlsx, .db)

## 分離する理由
- 入力ファイル(辞書)を誤って出力で上書きするリスクを排除
